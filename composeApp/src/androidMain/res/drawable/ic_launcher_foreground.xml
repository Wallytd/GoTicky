<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Add inset padding so the full logo is visible inside the adaptive mask -->
    <item>
        <inset
            android:drawable="@drawable/goticky_launcher"
            android:inset="16dp" />
    </item>
</layer-list>
